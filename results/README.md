# Reported Results

The CSV files in `reported/` contain the summary values reported in the
manuscript tables. They are provided in a machine-readable form for result
inspection and comparison. Raw logs and plotting utilities are not included.
